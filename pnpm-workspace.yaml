# pnpm 10+ moved settings out of package.json's "pnpm" field into this file.
# Approve native build scripts for wrangler's transitive deps so that
# `pnpm install --frozen-lockfile` doesn't fail with ERR_PNPM_IGNORED_BUILDS.
onlyBuiltDependencies:
  - esbuild
  - sharp
  - workerd
